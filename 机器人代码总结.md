整体模块
[main.py](D:/vvezre/clean/main.py)：主入口，包含 Flask 接口、Redis 状态初始化、串口控制、自动清扫、进出舱、低电回充、RTK 纠偏、线程启动等核心逻辑。文件很大，是当前项目的大脑。
[service.py](D:/vvezre/clean/service.py)、[layout_planner.py](D:/vvezre/clean/layout_planner.py)：根据光伏板区域/布局生成清扫路径 taskList，包含起终点、角度、航向、移动模式等。
[util.py](D:/vvezre/clean/util.py)：通用工具，包含 RTK 解析、经纬度距离/方位角计算、坐标转换、任务生成、串口查找等。
[RTKDataManager.py](D:/vvezre/clean/RTKDataManager.py)、[ntrip_runtime.py](D:/vvezre/clean/ntrip_runtime.py)、[rtk_correction.py](D:/vvezre/clean/rtk_correction.py)：RTK 定位、NTRIP 差分连接、航向角/横向偏差纠偏计算。
[vision_line_detection.py](D:/vvezre/clean/vision_line_detection.py)：OpenCV 视觉识别，主要识别亮带/竖线，用于辅助纠偏。
[mqtt_client.py](D:/vvezre/clean/mqtt_client.py)、[mqtt_integration.py](D:/vvezre/clean/mqtt_integration.py)、[mqtt_handler.py](D:/vvezre/clean/mqtt_handler.py)、[mqtt_vehicle_adapter.py](D:/vvezre/clean/mqtt_vehicle_adapter.py)：MQTT 云平台接入，接收云端指令并转成本地 /vehicle/... 接口调用。
[garage_state.py](D:/vvezre/clean/garage_state.py)、[battery_return.py](D:/vvezre/clean/battery_return.py)、[edge_target_guard.py](D:/vvezre/clean/edge_target_guard.py)：舱位状态、低电回充、到边保护等安全辅助逻辑。
[DynamicCronScheduler.py](D:/vvezre/clean/DynamicCronScheduler.py)：定时任务调度，比如定时自动清扫。
[tests](D:/vvezre/clean/tests)：测试覆盖 RTK、路径规划、舱位、低电、循环清扫、视觉识别、Python2 兼容等。
主要功能流程
服务启动后初始化 Redis 状态，启动摄像头线程、下位机串口监听、RTK 监听、RTK 看门狗、电量监听、MQTT，然后 Flask 监听 7899。
前端或云平台调用 /vehicle/autoDrive 启动自动清扫。
程序校验当前任务、RTK 坐标、任务起点、舱位状态。
如果车在舱内，先按固定距离出舱。
读取 [config.json](D:/vvezre/clean/config.json) 里的 taskList，逐段执行：转向、直行、滚刷、RTK 纠偏、到点判断。
RTK 数据持续进入 RTKDataManager，再由观察者函数更新 Redis、下发航向和纠偏指令。
任务完成后停车，按配置入舱，并发送充电命令。
如果低电或异常到边，会触发停车、回充或恢复逻辑。
配置/数据
[config.json](D:/vvezre/clean/config.json)：当前任务配置，包含起点、航向、充电桩、光伏板参数、taskList。
多个 001.json、电厂001.json、任务01.json：不同任务/场景配置。
[mqtt_config.json](D:/vvezre/clean/mqtt_config.json)：MQTT broker、topic、设备编号、上报字段。
[ntrip_config.example.json](D:/vvezre/clean/ntrip_config.example.json)：NTRIP 差分服务配置样例。